#include <stdio.h>   // Include the standard I/O library for printf

int main()
{
    // %d is a placeholder for an integer; it is replaced by the result (47) at runtime
    printf("23+24=%d\n",23+24);
    return 0;   // Return 0 means the program ends normally
}
