# factorial.c 逐行讲解

**这个程序干什么**：读入一个数 n，算出它的阶乘 n!（从 1 连续乘到 n）。

例：输入 5 → 输出 `5!=120`（1×2×3×4×5 = 120）

---

## 逐行讲解

### 第 1 行：进货

```c
#include <stdio.h>
```

把「标准输入输出仓库」搬进来。`printf` 和 `scanf` 都住在这个仓库里，不搬进来后面就用不了。

### 第 2~3 行：程序入口

```c
int main()
{
```

所有程序都从 `main` 开始跑。`{` 是开工大括号，干完活由最后的 `}` 收工。

### 第 4 行：开一个盒子

```c
int n;
```

声明一个装整数的盒子，取名 `n`，用来放要算阶乘的数。

### 第 6 行：读入 n

```c
scanf("%d",&n);
```

停下等你敲键盘。`%d` 表示读整数，`&` 表示「这个盒子的地址」，把读到的数放进 `n`。

### 第 7 行：开累乘器

```c
int fact = 1;
```

`fact` 是最终答案的盒子，**初始必须是 1，不是 0**。

为什么？因为它后面要被反复乘：如果初始是 0，`0 × 任何数 = 0`，乘一辈子都是 0。1 是乘法的「空盒子」——从 1 开始乘，第一轮才乘得出东西。

### 第 8 行：计数器（其实这行会被覆盖）

```c
int i = 1;
```

声明循环用的计数器 `i`。注意：这行的 `= 1` 其实**马上就会被覆盖**——因为 for 的第一间房会重新给它赋值（`i = n`）。所以这里写 `int i;` 也行。老师留着初始值，多半是习惯。

### 第 11 行：for 循环（核心）

```c
for(i=n;i>1;i--){
```

`for` 的括号里有**三间房**，用两个分号隔开：

```
for( i=n ; i>1 ; i-- )
    └┬─┘   └┬─┘   └┬─┘
   起点    条件    每轮末尾的动作
```

| 房间 | 内容 | 意思 |
|---|---|---|
| 第一间 | `i=n` | 循环开始前，让 i 从 n 出发 |
| 第二间 | `i>1` | 每轮进门先检查：i 还大于 1 吗？不大了就结束 |
| 第三间 | `i--` | 每轮干完活，i 自己减 1（就是 `i = i - 1` 的简写） |

**为什么是倒着数**（n → 2 而不是 1 → n）：乘法不在乎顺序，`5×4×3×2` 和 `1×2×3×4×5` 结果一样。而且 `i>1` 这个条件很妙——乘 1 等于白乘，所以在 2 就收手，少跑一轮没用的活。

### 第 12 行：滚雪球

```c
fact *= i;
```

镜子里读法：`fact = fact * i`——「fact 自己乘上 i，结果放回 fact」。

每一轮 fact 都变大一圈：

```
i=5 → fact = 1×5 = 5
i=4 → fact = 5×4 = 20
i=3 → fact = 20×3 = 60
i=2 → fact = 60×2 = 120
i=1 → 条件 i>1 不成立 → 循环结束
```

### 第 13 行：循环收工

```c
}
```

这个 `}` 是 for 的收工括号。花括号里的东西每轮重复执行。

### 第 14 行：打印结果

```c
printf("%d!=%d\n",n,fact);
```

逐个看引号里的内容：

| 内容 | 是什么 |
|---|---|
| `%d` | 第一个占位符 → 替换成 n |
| `!=` | **普通文字**！在 printf 的字符串里它不是「不等于」运算符，就是两个要打印的字符（感叹号 + 等号），原样显示 |
| `%d` | 第二个占位符 → 替换成 fact |
| `\n` | 换行 |

所以 `5!=120` 里的 `!=` 是「阶乘号」的样子，打印出来纯属装饰，和比较运算符 `!=` 毫无关系。

### 第 15 行：收工

```c
return 0;
```

告诉系统：正常结束。`0` 表示「没问题」。

---

## 为什么 0 和 1 也天然正确（边界情况）

| 输入 | for 循环 | 结果 |
|---|---|---|
| `0` | 第一间房 `i=0`，第二间房检查 `0>1` 不成立 → 循环一次都不跑 | `fact` 保持 1 → `0!=1` ✅（数学上 0 的阶乘就是 1） |
| `1` | `i=1`，`1>1` 不成立 → 不跑 | `fact` 保持 1 → `1!=1` ✅ |
| `5` | 正常滚雪球 | `120` ✅ |

循环不跑时，答案就是 `fact` 的初始值 1——正好撞上正确答案。这是这个写法的一个隐藏优点。

---

## 附：如果写成「正着乘」版本

```c
for(i=1;i<=n;i++){
    fact *= i;
}
```

结果完全一样。区别只是：
- 正着乘：`i<=n` 要走到 n 才停，多乘一次 1（白干一轮）
- 倒着乘：`i>1` 在 2 就收手，省一轮

两种都正确，面试写哪种都行。老师教倒着乘，是因为它连「多乘一次 1」这种小浪费都避免了。

---

## 一句话总结

> **开累乘器（初始 1）→ for 从 n 倒着数到 2 → 每轮 `fact *= i` 滚雪球 → 打印 `n!=fact`。**
