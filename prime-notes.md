# prime.c 逐行讲解 —— 判断素数

**这个程序干什么**：读入一个数 x，判断它是不是素数（质数），打印 `prime` 或 `not prime`。

**素数的定义**：只能被 1 和它自己整除的大于 1 的整数。
- 2、3、5、7、97 是素数
- 4、8、100 不是（还能被别的数整除）
- **1 不是素数**（这点很重要，后面会讲）

---

## 核心思路：假设法（旗帜 flag）

这个程序用的是一套非常经典的套路，叫「**先假设，再找茬**」：

1. **先假设**：`isPrime = 1`（我先假设你是素数）
2. **找茬**：从 2 开始，挨个试能不能整除 x
3. **打脸**：只要找到一个能整除的，就说明假设错了 → `isPrime = 0`
4. **宣判**：最后看旗帜还立着没

`isPrime` 这个变量叫「**旗帜**」（英文 flag）——举着 = 是素数，倒了 = 不是。这个套路你以后会天天见到。

---

## 逐行讲解

### 第 1~3 行：进货 + 开工

```c
#include <stdio.h>    // 搬进 printf 和 scanf
int main()
{                     // 程序入口，开工
```

### 第 4~5 行：读入要判断的数

```c
int x;              // 开个盒子，装待判断的数
scanf("%d",&x);     // 从键盘读一个整数放进去
```

### 第 7~8 行：开计数器 + 立旗

```c
int i;                  // 循环用的计数器
int isPrime = 1;        // 旗帜：1 = 假设是素数（初始立场）
```

### 第 9 行：for 循环 —— 挨个找茬

```c
for (i=2;i<x;i++){
```

三间房拆开看：

| 房间 | 内容 | 意思 |
|---|---|---|
| 第一间 | `i=2` | 从 2 开始试（因为任何数都能被 1 整除，试 1 没意义） |
| 第二间 | `i<x` | 试到 x-1 为止（不用试 x 自己，它当然能整除自己） |
| 第三间 | `i++` | 每轮 i 加 1，往下试 |

**为什么从 2 开始、到 x-1 结束**：这段范围就是「可能存在的其他因子」。如果这个范围内一个都没有，x 就只能是素数。

### 第 10~13 行：找到茬就打脸

```c
if(x % i == 0){        // x 能被 i 整除吗？
    isPrime = 0;       // 能 → 说明有别的因子 → 不是素数，放倒旗帜
    break;             // 不用再试了，提前收工
}
```

**`x % i == 0` 是什么意思**：`%` 是取余数。如果 `x` 除以 `i` 的余数是 0，就说明「除得干干净净」，i 是 x 的因子。

比如 x=8：
```
8 % 2 = 0 → 找到了！2 能整除 8 → 8 不是素数
```

**`break` 是干什么的**：跳出循环。既然已经确定它不是素数了，再往下试就是白费力气——比如 8 在 i=2 时就被判定完了，没必要再试 3、4、5、6、7。

**⚠️ 这里的 `=` 是单等号**——是「赋值」不是「比较」。写 `isPrime == 0;` 是常见错误，那句只是「问一下」，问完什么都没改变。

### 第 16~21 行：宣判

```c
if(isPrime == 1){         // 旗帜还立着 → 没找到任何因子
    printf("prime\n");
}
else{                     // 旗帜倒了 → 找到过因子
    printf("not prime\n");
}
```

注意这里是 `==`（双等号），因为这是**判断**，不是赋值。

**⚠️ 两个分支必须打印不同的内容**——写错了容易变成两边都打印 "not prime"，程序就废了（而且输入合数时还碰巧显示正确，很难发现）。

### 第 22~23 行：收工

```c
return 0;     // 正常结束
}
```

---

## 用 7 和 8 各走一遍

### 输入 7（素数）

| i | 检查 `7 % i == 0` | 结果 |
|---|---|---|
| 2 | 7%2 = 1 | 不是，继续 |
| 3 | 7%3 = 1 | 不是，继续 |
| 4 | 7%4 = 3 | 不是，继续 |
| 5 | 7%5 = 2 | 不是，继续 |
| 6 | 7%6 = 1 | 不是，继续 |
| — | i=7，条件 `7<7` 假 → 循环结束 | |

循环跑完，`isPrime` 一直是 1 → 打印 **prime** ✅

### 输入 8（合数）

| i | 检查 `8 % i == 0` | 结果 |
|---|---|---|
| 2 | 8%2 = **0** | 找到了！→ `isPrime = 0` → `break` 跳出 |

其他 i 根本没试（break 提前收工）→ 打印 **not prime** ✅

---

## ⚠️ 边界 bug：输入 0 或 1 会判错

实测结果：

| 输入 | 程序输出 | 正确答案 | 对不对 |
|---|---|---|---|
| 7 | prime | prime | ✅ |
| 8 | not prime | not prime | ✅ |
| 2 | prime | prime | ✅ |
| 97 | prime | prime | ✅ |
| 100 | not prime | not prime | ✅ |
| **1** | **prime** | **not prime** | ❌ |
| **0** | **prime** | **not prime** | ❌ |

**为什么错**：看 for 的第二个条件 `i<x`。

- 输入 1：`i=2`，检查 `2<1` → 假 → **循环一次都不跑**
- 循环不跑，就没人去找茬 → `isPrime` 保持初始值 1 → 判成「素数」

**1 和 0 按数学定义都不是素数**（素数必须大于 1），但程序把它们当成素数了。

**修法**：在开头加一道「卫生检查」：

```c
if(x < 2){
    printf("not prime\n");
    return 0;          // 直接结束，不用往下走了
}
```

放在 `scanf` 后面。这样 0、1 以及负数都会被提前拦下。

> 这是「**边界值测试**」的又一个实例——你之前测 0! 和 1! 也是同一个道理。程序写完后，记得专门想想「输入最特殊的值会怎样」。

---

## 想优化的话（进阶，了解即可）

现在循环要试到 x-1，x 很大时比较慢。其实**试到 √x 就够了**（数学上可以证明：如果一个数有因子，必然有一个因子 ≤ √x）。要改成：

```c
for (i=2; i*i <= x; i++){
```

`i*i <= x` 就是「i 不超过根号 x」的写法（不用开根号，两边平方就行）。这个优化在竞赛和面试里常见，现在知道有这么回事就好。

---

## 一句话总结

> **立旗（假设是素数）→ for 从 2 试到 x-1 找茬 → 找到因子就放倒旗帜并 break → 最后看旗帜宣判。**

核心是两个今天的新东西：
- **旗帜 flag**：用一个变量记住「当前状态」，最后统一判断
- **break**：条件满足时提前跳出循环，不白干活
